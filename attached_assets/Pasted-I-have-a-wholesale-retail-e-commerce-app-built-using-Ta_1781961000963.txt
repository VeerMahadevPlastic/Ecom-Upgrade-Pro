I have a wholesale/retail e-commerce app built using Tailwind CSS, HTML, and JavaScript. I want to completely upgrade its UI/UX and behavior to match the premium, highly-polished standards of modern platforms like Amazon, keeping it 100% responsive and clean. Please perform the following updates on my codebase:

1. Amazon-Inspired Navigation & Header:
   - Create a sticky, premium, dark emerald or charcoal-gray top navigation bar.
   - Put a high-visibility, large modern search input in the center with an adjacent category dropdown built into the bar.
   - The search must trigger instant, smooth client-side or backend filtering over the current product list as the user types (live search auto-suggestions).
   - Add clear, clean icons for "Cart" (with a live floating count badge) and a "B2B Login" button that opens the modern modal elegantly.

2. Premium Product Cards (Amazon-Style Grid):
   - Redesign the #productGrid cards into sleek white cards with subtle shadows, a very thin border, and lots of clean padding.
   - Ensure product images are rendered beautifully in a structured aspect-ratio box (like square aspect-square) with smooth zoom or scale hover effects.
   - Display product metadata professionally: Product Name in bold, Category in small muted text, Stock Status as a subtle badge ("In Stock" in green), and tier-pricing displayed clearly (e.g., "₹120 / unit for Bulk").
   - Include an explicit, stylish "Add to Cart" button at the bottom of every product card that changes state dynamically when clicked.

3. Interactive Slide-out Cart Drawer & Modern Modals:
   - Make sure the #cartDrawer uses a smooth slide-out transition from the right edge with a clean backdrop overlay.
   - Format the checkout details elegantly in a multi-step accordion or clear wizard within the modal (Summary -> Address & Details -> Payment/UPI Options).
   - Clean up the form fields to look like premium inputs with soft focus borders.

4. Modern Layout Adjustments:
   - The Shipping Estimate widget should be moved to a clean secondary sidebar or a dedicated tab so the main Hero section stays uncluttered and focuses fully on the main value proposition and primary Call-to-Actions (CTAs).
   - Use consistent font weights, generous line-heights, and clear black text for high readability.

Please review the current logic (including how categories and products are generated) and carefully inject these UI enhancements without breaking the existing WhatsApp workflow or UPI billing functions. Let's execute these frontend and design upgrades now.